// Tailwind v4 CSS-first 스텁 - 실제 토큰은 app/globals.css @theme 에 정의
import type { Config } from "tailwindcss";

// v4 에서는 content/theme 설정이 CSS @source / @theme 로 옮겨감. 본 파일은 IDE 인식용 stub.
const config: Config = {};

export default config;
