// Tailwind v4 stub — 실제 토큰은 app/globals.css 의 @theme inline 에서 정의 (CSS-first)
import type { Config } from "tailwindcss";

const config: Config = {
  // v4 는 content 자동 감지 (@source directive in globals.css)
  // 이 파일은 IDE 자동완성·TS 검증용 stub
  theme: {},
  plugins: [],
} satisfies Config;

export default config;
