// web 앱 Tailwind 설정 — Tailwind v4 CSS-first. content/source 는 globals.css 의 @source directive 로 옮겼고, 토큰은 @theme inline 으로 정의. 본 파일은 호환성 유지용 stub.
import type { Config } from "tailwindcss";

const config: Config = {
  content: [],
};

export default config;
