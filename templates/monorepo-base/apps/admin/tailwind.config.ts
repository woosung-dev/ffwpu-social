// admin Tailwind v4 설정 — CSS-first. content/source 는 globals.css 의 @source directive 로. 토큰은 @theme inline.
import type { Config } from "tailwindcss";

const config: Config = {
  content: [],
};

export default config;
